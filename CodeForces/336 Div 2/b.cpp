#include <iostream>
#include <fstream>
#include <vector>
#include <string.h>
#include <sstream>
#include <algorithm>
#include <string>
#include <unordered_map>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string a;
    cin >> a;
    string b;
    cin >> b;
    int totalSum = 0;
    int aL = a.length();
    int bL = b.length();
    int stoppingPoint = bL - aL;
    string c = "110";
    cout << '1' - '0' << endl;

}
