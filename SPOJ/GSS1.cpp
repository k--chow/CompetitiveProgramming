#include <algorithm>
#include <vector>
#include <string>
#include <iostream>
#include <math.h>
#include <stack>
using namespace std;

class SegmentTree
{
    vector<int> values
    vector<int> left;
    vector<int> right;

    int query(int left, int right)
    {
        return 0;
    }
};

//recursively build the segment tree
void build()
{

}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    //segment tree
    //range maximum query
}
