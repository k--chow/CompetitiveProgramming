#include <algorithm>
#include <vector>
#include <tuple>
#include <string>
#include <iostream>
#include <math.h>
#include <stack>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <cstdlib>
#include <queue>
#include <iomanip>
#include <map>
using namespace std;
typedef vector< vector<int> > vii;
typedef pair<int, int> pii;

int main()
{
  vector<int> inputs;
  while (!cin.eof()) {
    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    if (a == 0 || b == 0 || c == 0 || d == 0 || e == 0 || f == 0)
    {
      cout << "NO" << endl;
    }
    else if (pow(a, 2) + pow(b, 2) == pow(c, 2) ||
  pow(a,2) + pow(c, 2) == pow(b, 2) || pow(c,2) + pow(b,2) == pow(a, 2)) {
      vector<int> a, b;
      a.push_back()
      if (mappy.count(d) == 1 && mappy.count(e) == 1 && mappy.count(f) == 1)
      {
        cout << "YES" << endl;
      }
       else {
         cout << "NO" << endl;
       }
    } else {
    cout << "NO" << endl;
  }

  }
}
